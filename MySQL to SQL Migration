1. Setup XAMPP
2. Open XAMPP Control Panel and press on Config button.
if 80 port is failed to open then open in 8080 port. 
for this go to Apache Server right side Config button>
Apache(httpd.config)- change the port 80 to 8080 in this file. Save and close.
3. check http://localhost:8080/phpmyadmin
4. if it shows the admin panel then create new database 
and create tables from given schema then insert data(if given)
--------
5. Install SSMAforMySQL_8.16.0 (64 bit),
6. If needed install VC_redist.x64
7. then install mysql-connector-odbc-8.0.23-winx64
8. go to SQL Server Migration Assistant- 
Click on Reconnect to MySQL-Initially it will create auto a database (if not exists)
Driver: MySQL ODBC 8.0 Unicode Driver (v8.0.23)
Server name:localhost
Server port:3306
User name:root
press Connect, it will connect and create a database.
9. Press to Reconnect to SQL Server- it will connect to SQL databse to migrate
10. check both database from MySQL and SQL (from and to database)
11. select mysql db and click on Convert Schema, it will create schema
12. press Migrate Data- it will migrate data from mysql to sql and will show a report
check the report is ok or not.
its done.
